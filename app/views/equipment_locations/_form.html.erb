<%= form_with(model: equipment_location) do |form| %>
  <% if equipment_location.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(equipment_location.errors.count, "error") %> prohibited this equipment_location from being saved:</h2>

      <ul>
        <% equipment_location.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :equipment_id, style: "display: block" %>
    <%= form.text_field :equipment_id %>
  </div>

  <div>
    <%= form.label :location_id, style: "display: block" %>
    <%= form.text_field :location_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
