<p style="color: green"><%= notice %></p>

<h1>Creatures</h1>

<div class="compendium-container">
  <div id ="all-entries">
    <div class="row wrap">
      <% @creatures.each do |creature| %>
        <%= render creature %>
        </div>
      <% end %>
    </div>
  </div>
</div>
