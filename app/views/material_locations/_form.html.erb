<%= form_with(model: material_location) do |form| %>
  <% if material_location.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(material_location.errors.count, "error") %> prohibited this material_location from being saved:</h2>

      <ul>
        <% material_location.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :material_id, style: "display: block" %>
    <%= form.text_field :material_id %>
  </div>

  <div>
    <%= form.label :location_id, style: "display: block" %>
    <%= form.text_field :location_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
