<%= form_with(model: monster_drop) do |form| %>
  <% if monster_drop.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(monster_drop.errors.count, "error") %> prohibited this monster_drop from being saved:</h2>

      <ul>
        <% monster_drop.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :monster_id, style: "display: block" %>
    <%= form.text_field :monster_id %>
  </div>

  <div>
    <%= form.label :drop_id, style: "display: block" %>
    <%= form.text_field :drop_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
