<%= form_with(model: treasure_location) do |form| %>
  <% if treasure_location.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(treasure_location.errors.count, "error") %> prohibited this treasure_location from being saved:</h2>

      <ul>
        <% treasure_location.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :treasure_id, style: "display: block" %>
    <%= form.text_field :treasure_id %>
  </div>

  <div>
    <%= form.label :location_id, style: "display: block" %>
    <%= form.text_field :location_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
