<p style="color: green"><%= notice %></p>

<%= render @equipment_property %>

<div>
  <%= link_to "Edit this equipment property", edit_equipment_property_path(@equipment_property) %> |
  <%= link_to "Back to equipment properties", equipment_properties_path %>

  <%= button_to "Destroy this equipment property", @equipment_property, method: :delete %>
</div>
