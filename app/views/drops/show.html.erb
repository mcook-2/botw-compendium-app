<p style="color: green"><%= notice %></p>

<%= render @drop %>

<div>
  <%= link_to "Edit this drop", edit_drop_path(@drop) %> |
  <%= link_to "Back to drops", drops_path %>

  <%= button_to "Destroy this drop", @drop, method: :delete %>
</div>
